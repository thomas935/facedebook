import javax.swing.*;

public class optionUserLambda {
    private JPanel panel1;

    public optionUserLambda(){
        JFrame frame = new JFrame();
        frame.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        frame.setSize(800, 600);
        frame.setVisible(true);
    }
}
